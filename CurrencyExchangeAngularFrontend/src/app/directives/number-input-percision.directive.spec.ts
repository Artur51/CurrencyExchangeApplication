/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { NumberFixPercisionDirective } from './number-input-percision.directive';

describe('Directive: NumberFixPercision', () => {
  it('should create an instance', () => {
    const directive = new NumberFixPercisionDirective();
    expect(directive).toBeTruthy();
  });
});
