import { FilterExcludeValuePipe } from './filter-exclude-value.pipe';

describe('FilterExcludeValuePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterExcludeValuePipe();
    expect(pipe).toBeTruthy();
  });
});
